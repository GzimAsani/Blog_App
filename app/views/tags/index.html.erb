<h1>All Tags</h1>

<ul id="tag">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, article_path(tag) %>
    </li>
  <% end %>
</ul>

<%= link_to "Create a New Tag", new_article_path, class: "new_tag" %>